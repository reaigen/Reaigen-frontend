"use client";

import { PageLoading } from "../../../components/page-loading";

/** Immediate paint while the editor route streams in. */
export default function DraftFloorplanLoading() {
  return <PageLoading />;
}
